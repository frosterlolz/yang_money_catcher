{
  "ENVIRONMENT": "${ENVIRONMENT}",
  "BASE_URL": "${BASE_URL}",
  "AUTH_TOKEN": "${AUTH_TOKEN}",
  "SYNC_ATTEMPTS": "${SYNC_ATTEMPTS}",
  "SKIP_AUTH": "${SKIP_AUTH}",
  "USE_MOCKS": "${USE_MOCKS}"
}